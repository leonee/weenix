in vref()
    - what does it mean for a filesystem's linkcount to be 0? 

in lookup()
    -dafuq does lookup return?

in do_open()
    - how do we create a vnode?

in open_namev()
    - what do we do about errors?
    - should we be locking?

in do_write()
    - if the file is extended, do we need to manually modify the len field?

In general:
    - Where do we set a proc's cwd?
